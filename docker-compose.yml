version: '2.0'
services:
  mysql-server:
    image: mysql:5.7
    ports:
      - '3306:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=pass
      - MYSQL_USER=4bank
      - MYSQL_PASSWORD=4bank
      - MYSQL_DATABASE=4bankdb
volumes:
  mysql_data:
    driver: local
